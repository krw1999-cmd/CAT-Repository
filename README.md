# CAT Automation

A personal automation system that replaces a manual side-hustle workflow with small, composable Python tools.

Each tool handles one step in the process and is triggered automatically when files appear in a watched folder. Tools are built with Claude Code and run independently — no Claude API calls at runtime.

See `CLAUDE.md` for the full project guide and `tools/inventory.md` for a list of all tools.
